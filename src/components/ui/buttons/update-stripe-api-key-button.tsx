'use client'
import { Button } from "../button";

export default function UpdateStripeApiKeyButton() {
    return (
        <Button variant={'outline'} formAction={() => { }} >Update</Button>
    );
}